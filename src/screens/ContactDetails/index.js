import { useNavigation } from '@react-navigation/native';
import React, { useEffect } from 'react';
import { Text, View } from 'react-native';

const ContactDetails = () => {

    return (
        <View>
            <Text>My Contacts Details</Text>
        </View>
    )
}

export default ContactDetails;
